// Преобразование типов
// Из строки в boolean

let match = "хуй";
alert(typeof match);

match = String("Украина");
alert(typeof match);

// из строки в Number
let ext = "привет";

tex = Boolean(ext);
alert(typeof tex);